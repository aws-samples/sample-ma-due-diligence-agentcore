# Top-level pinned dependencies for the M&A Due Diligence AgentCore sample.
# Keep this file in sync with the container image build (infra/agent_image/Dockerfile)
# and with any Lambda handlers that need runtime dependencies.

# Infrastructure as Code
aws-cdk-lib==2.259.0
constructs==10.6.0

# AWS SDKs
# Kept in sync with ``lambda/requirements.txt`` so the handlers and
# the reader's local venv resolve the same service models.
boto3==1.42.37
botocore==1.42.37

# Agent frameworks
strands-agents>=1.0.0
bedrock-agentcore>=1.0.0

# Document generation (synthetic CIMs, financials, press packs)
reportlab==4.2.5

# Aurora PostgreSQL driver (used by data generator via RDS Data API fallback)
psycopg[binary]==3.2.3

# Dev tooling
ruff==0.8.4
pytest==8.3.4
pytest-mock==3.14.0
